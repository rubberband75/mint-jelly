id=docker-engine
name=Docker Engine
description=Install Docker Engine from Docker's signed Ubuntu APT repository with non-sudo access for the desktop user
network=required
privilege=system
interactive=no
architectures=amd64 arm64 armhf s390x ppc64el
verification=signature-required
lifecycle=install-update-uninstall-purge
